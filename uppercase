# Uppercase input string

# Example 1. Uppercase a string.
# $ echo "Foo" | uppercase

# Example 2: Uppercase all lines in a file
# cat file.txt | xargs -I {} uppercase {}

if [ -z "$1" ]
    then
        read arg
    else
        arg=$1
fi
res=$(echo $arg | awk '{print toupper($0)}')
printf "$res\n"
